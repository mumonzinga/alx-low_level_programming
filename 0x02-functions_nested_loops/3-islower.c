#include "main.h"
#include <stdio.h>

/**
 * _islower - prints 1 or 0 depending on input
 *
 * Description: prints all lowercase letters
 *		c - parameter
 *
 * Return: Always(0).
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
